using System;

namespace Core.Nhibernate.IntegrationTests
{
    internal static class TestConstants
    {
        public static readonly TimeSpan AllowableTimeVariance = TimeSpan.FromSeconds(1);
    }
}
